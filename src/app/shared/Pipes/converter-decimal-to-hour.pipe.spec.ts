import { ConverterDecimalToHourPipe } from './converter-decimal-to-hour.pipe';

describe('ConverterDecimalToHourPipe', () => {
  it('create an instance', () => {
    const pipe = new ConverterDecimalToHourPipe();
    expect(pipe).toBeTruthy();
  });
});
